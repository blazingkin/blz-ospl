:has_key?(hash, key)
	BLZINTERNALHASHHASKEY hash, key, "result"
	return result
end

:set(hash, key, value)
	hash[key] = value
	return hash
end

:get(hash, key)
	if hash.has_key?(key)
		return hash[key]
	else
		return {blz.nil}
	end
end

:nil?
	return false
end	
