constructor Expect(value)

    :to_be(other)
        if value != other
            print("Assertion Failed!")
            print("Expected "+value+" to be "+other)
            exit(1)
			return false
        end
        return true
    end


end

:expect(value)
    Expect(value)
end